<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "KiwiFruit" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class='nav-color-bar'><%= image_tag 'nav_top_color_bar.png' %></div>
    <div class='nav-top'><%= render 'layouts/nav_top' %></div>
    <div class='nav-main'><%= render 'layouts/nav_main' %></div>
    <div class="container">
      <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
